# Release history

## 0.1.1-umbrel.1
- Initial namespaced KUNAS package, pinned upstream source, full Vite runtime.
- Persistent provider configuration and cache, authenticated same-origin setup.
- Dual-architecture GHCR CI with unit/build/runtime checks.
